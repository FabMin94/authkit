[project]
name = "authkit"
version = "0.1.0"
description = "A standalone authentication microservice"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn>=0.47.0",
]
